{
    "version": 2,
    "builds": [
        {
            "buildCommand": "vercel build",
            "src": "./dist/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/app.js"
        }
    ]
}